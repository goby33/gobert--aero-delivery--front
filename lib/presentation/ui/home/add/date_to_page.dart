import 'package:flutter/material.dart';

class DateToPage extends StatelessWidget {
  DateToPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("DateToPage"));
  }
}
