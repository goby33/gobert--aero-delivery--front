import 'package:flutter/material.dart';

class ResumeTripPage extends StatelessWidget {
  ResumeTripPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("ResumeTripPage"));
  }
}
