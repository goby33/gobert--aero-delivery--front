import 'package:flutter/material.dart';

class DateFromPage extends StatelessWidget {
  DateFromPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("DateFromPage"));
  }
}
